spring:
  kafka:
    bootstrap-servers: localhost:9092
    security:
      protocol: PLAINTEXT

springdoc:
  api-docs:
    enabled: true
  swagger-ui:
    enabled: true
    path: /swagger-ui.html

ingestion:
  kafka:
    topic: test.order.events.v1
  cors:
    allowed-origins:
      - http://test.example
